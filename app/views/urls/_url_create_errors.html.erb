
	<% if @url.errors.any? %>
	<div id="errorExplanation">
		<h2><%= pluralize(@url.errors.count, "error") %> prohibited this url from being created:</h2>
		<ul>
			<% @url.errors.full_messages.each do |message| %>
			<li><%= message %></li>
		</ul>
	</div>
	<% end %>
